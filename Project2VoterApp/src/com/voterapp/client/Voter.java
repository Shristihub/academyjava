package com.voterapp.client;

public class Voter {

	public static void main(String[] args) {
		//Use SCanner get values for age, voterid, locality one by one
		//create an object of electionBoothServiceImpl
		// interface ref =  implementation class
		//call the method of electionBoothServiceImpl
		// use try catch and handle the specific exception
		//if age does not match handle exception and terminate, else
		//If locality does not match, handle exception and terminate, else
		//If voterid does not match, handle exception and terminate, else
		//If everything is perfect, then Print “You are eligible to vote”
	}

}
